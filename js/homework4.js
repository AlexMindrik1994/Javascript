/*------------------------ЗАДАЧА 1------------------------------*/


/*let sum = prompt('Введите числа')
function getSumNumber(num) {
	     let sum = 0;
	    let tmp;

	    while (num) {
	        tmp = num % 10;
	        num = (num - tmp) / 10;
	        sum += tmp;
	    }
	    return sum;

	}
	alert('Сумма чисел\n' + getSumNumber(sum));*/
/*---------------------------------------------------------------*/


/*----------------------------------ЗАДАЧА 3-------------------------*/
	/*let pwd = prompt ("Введите пароль");


	for (let i = 0; i < pwd.length; i++ ) {

		if (pwd.length < 9) {
			alert ("Введите пароль из 9 символов")
		}
		let regExp = /\w\d\s/;
			if (regExp.test(pwd)){
				alert("Password введён правильно!");
			} else {
				alert("Password введён неправильно!");
	}
	}*/
/*----------------------------------------------------------------------*/



